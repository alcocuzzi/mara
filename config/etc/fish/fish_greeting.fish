function fish_greeting
   set_color --bold green
   echo "Welcome to mara, built to enhance the experience on AWS!"
   echo ""
   set_color magenta
   echo "Tools included: awscli, argocd cli, boto3, botocore, checkov, curl, fixuid, git, hadolint, jq, kubectl, k9s, pip3, pre-commit, python3, ssm-session-manager, starship, tar, terraform, terraform-docs, unzip, velero cli, vim, nc(netcat), wget and more."
   echo ""
   set_color --bold green
   echo "Type 'mara' and press enter to explore available commands."
end
